import 'package:hive/hive.dart';

extension ActiveRecordRelation on BoxBase {
  find(int id) {
//    checkOpen();
    print("find!!!");
//    return keystore.getValues().firstWhere((record) => record.id == id);
  }

  hogehoge() {
    print("hogehxxxxoge");
  }
}
